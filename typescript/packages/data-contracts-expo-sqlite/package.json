{
  "name": "@baukit/data-contracts-expo-sqlite",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "pnpm run build",
    "test": "vitest run --config ../../vitest.config.ts",
    "lint": "eslint src --max-warnings=0",
    "format:check": "prettier --check src package.json tsconfig.json"
  },
  "devDependencies": {
    "@baukit/data-contracts": "workspace:*",
    "expo-sqlite": "57.0.1",
    "typescript": "6.0.3"
  },
  "peerDependencies": {
    "@baukit/data-contracts": "^0.3.3",
    "expo-sqlite": "^57.0.1"
  }
}
