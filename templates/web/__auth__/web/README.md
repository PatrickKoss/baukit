# {{ context.app_name }} web

Vite, React, and TanStack Query application with OIDC authorization-code + PKCE authentication, generated by Baukit template {{ context.template_version }}.

Run `docker compose up -d --wait keycloak` from the product root, copy `.env.example` to `.env`, then run `corepack pnpm@11.18.0 install --frozen-lockfile && corepack pnpm@11.18.0 dev`. Sign in with `test` / `password`. The browser stores the short-lived token set locally, refreshes before expiry, and uses Keycloak's end-session endpoint for logout. This template deliberately opts into `offline_access`; remove `offlineAccess: true` if the deployed provider does not issue offline refresh tokens. No client secret is shipped to the browser.{% if context.port_offset > 0 %}

`VITE_API_URL` defaults to `http://localhost:{{ context.api_host_port }}` and `VITE_OIDC_ISSUER` defaults to Keycloak on port {{ context.keycloak_host_port }}.{% endif %}

Protected API calls use the runtime's explicit one-replay 401 handshake. Concurrent callers share refresh; terminal rejection clears auth and disables authenticated schedulers, while transient provider/network failure preserves the session for retry. A second 401 stops. Replay does not make writes safe by itself, so every replayable mutation still needs server-side idempotency.

The backend-confirmed immutable OIDC subject is selected before user-scoped queries and analytics identity are enabled. Account changes and terminal expiry reset query state and analytics first; a corrupt ownership registry blocks instead of falling back to shared state. The starter is fail-closed on an offline reload until it can confirm the token's subject because `@baukit/auth-web` deliberately does not trust decoded token claims. The generated logout policy is **retain**; review [docs/local-data-retention.md](docs/local-data-retention.md) before adding Dexie or changing retention.

Use the same issuer hostname everywhere. The dynamic local Keycloak advertises whichever of `localhost` or `127.0.0.1` was used for discovery; token validation requires an exact issuer match. Prefer `localhost` in browsers because development login cookies marked `Secure` receive special localhost handling. Production must use HTTPS.

The generated target also includes local interaction references: a same-tick mutation mutex, focus trap and inert-background dialog, mutually exclusive detail-route states, safe back-or-replace navigation, linked live validation, semantic headings, visible keyboard focus, and 44-by-44 CSS-pixel minimum targets. See the helpers and their acceptance tests under `src/`; keep server-side idempotency for network mutations even when the UI is single-flight.

The Baukit packages come from {{ context.baukit_typescript_dependency_description }} and the generated lockfile makes the first install reproducible. The app runs without a globally installed Baukit CLI.

Run `corepack pnpm@11.18.0 build`, `corepack pnpm@11.18.0 lint`, and `corepack pnpm@11.18.0 test` before shipping.
